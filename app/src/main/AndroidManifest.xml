<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.johnmillercoding.hometrashaudit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".services.VolleyController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.LoginActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.HomeScreenActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.AuditActivity"
            android:label="New Audit"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.JournalActivity"
            android:label="Your Journal"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.TotalsActivity"
            android:label="Totals"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.WasteSelectionActivity"
            android:label="Waste Selection"
            android:screenOrientation="portrait" />
        <activity android:name=".activities.UpdateActivity"></activity>
    </application>

</manifest>